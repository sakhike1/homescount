/** Shared max width for homepage hero section (card, nav). */
export const HOME_HERO_CONTAINER = 'max-w-[90rem] mx-auto w-full'

/** Narrower centered width for hero stats below the card. */
export const HOME_STATS_CONTAINER = 'max-w-4xl mx-auto w-full'
